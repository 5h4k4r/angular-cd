resources:
  - ../../base
patches:
  - path: ingress-patch.yaml
    target:
      kind: Ingress
      name: angular

commonLabels:
  app: staging-angular
namespace: staging
configMapGenerator:
  - behavior: merge
    literals:
      - ENV=staging
    name: angular
